import 'package:flutter/material.dart';



class FooterBot extends StatelessWidget {
  const FooterBot({
    required Key key,
  }):super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}